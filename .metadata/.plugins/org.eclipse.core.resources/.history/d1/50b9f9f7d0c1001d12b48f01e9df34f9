package com.fitbuddy.Dao;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import com.fitbuddy.Model.CompKey;
import com.fitbuddy.Model.DailyActivity;

@Repository
public interface DailyActivityDao extends JpaRepository<DailyActivity, CompKey> {

	@Query("SELECT e FROM DailyActivity e WHERE e.ck.uid = :ck.uid AND e.ck.date = :ck.date GROUP BY e.ck.uid ORDER BY e.ck.date DESC limit(1,5)")
    List<DailyActivity> findLast5RecordsByCompositeKey(@Param("uid") CompKey uid, @Param("data") CompKey date);
	
}
